apiVersion: v1
kind: Service
metadata:
  name: transport
  labels:
    app: transport
    version: {{ .Chart.Version }}
spec:
  type: ClusterIP
  selector:
    app: transport
    version: {{ .Chart.Version }}
  ports:
  - port: 80
    name: http
