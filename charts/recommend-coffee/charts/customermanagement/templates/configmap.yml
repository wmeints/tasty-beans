apiVersion: v1
kind: ConfigMap
metadata:
  name: customermanagement-config
data:
    telemetry.endpoint: "http://jaeger-collector.istio-system:14268/api/traces"
    telemetry.name: customermanagement.default
    telemetry.version: "{{ .Chart.Version }}"