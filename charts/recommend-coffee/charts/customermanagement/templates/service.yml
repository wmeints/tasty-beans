apiVersion: v1
kind: Service
metadata:
  name: customermanagement
  labels:
    app: customermanagement
    version: {{ .Chart.Version }}
spec:
  type: ClusterIP
  selector:
    app: customermanagement
    version: {{ .Chart.Version }}
  ports:
  - port: 80
    targetPort: 80
