apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: customermanagement-virtual-service
spec:
  hosts:
  - "recommend.coffee"
  gateways:
  - recommendcoffee-gateway
  http:
  - name: customermanagement-route
    match:
      - uri:
          prefix: /customermanagement/
    rewrite:
      uri: /
    route:
    - destination:
        host: customermanagement.default.svc.cluster.local
        port:
          number: 80