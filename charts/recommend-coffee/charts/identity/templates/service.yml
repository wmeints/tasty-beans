apiVersion: v1
kind: Service
metadata:
  name: identity
  labels:
    app: identity
    version: {{ .Chart.Version }}
spec:
  type: ClusterIP
  selector:
    app: identity
    version: {{ .Chart.Version }}
  ports:
  - port: 80
    targetPort: 80
