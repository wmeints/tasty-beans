apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: identity-virtual-service
spec:
  hosts:
  - "identity.recommend.coffee"
  gateways:
  - identity-gateway
  http:
  - name: identity-route
    match:
      - uri:
          prefix: /
    route:
    - destination:
        host: identity
        port:
          number: 80