apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: subscriptions-virtual-service
spec:
  hosts:
  - "recommend.coffee"
  gateways:
  - recommendcoffee-gateway
  http:
  - name: subscriptions-route
    match:
      - uri:
          prefix: /subscriptions/
    rewrite:
      uri: /
    route:
    - destination:
        host: subscriptions.default.svc.cluster.local
        port:
          number: 80