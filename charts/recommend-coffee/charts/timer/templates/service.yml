apiVersion: v1
kind: Service
metadata:
  name: timer
  labels:
    app: timer
    version: {{ .Chart.Version }}
spec:
  type: ClusterIP
  selector:
    app: timer
    version: {{ .Chart.Version }}
  ports:
  - port: 80
    name: http
