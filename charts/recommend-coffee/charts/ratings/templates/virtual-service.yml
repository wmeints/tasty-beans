apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: ratings-virtual-service
spec:
  hosts:
  - "recommend.coffee"
  gateways:
  - recommendcoffee-gateway
  http:
  - name: ratings-route
    match:
      - uri:
          prefix: /ratings/
    rewrite:
      uri: /
    route:
    - destination:
        host: ratings.default.svc.cluster.local
        port:
          number: 80