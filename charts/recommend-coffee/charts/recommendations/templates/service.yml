apiVersion: v1
kind: Service
metadata:
  name: recommendations
  labels:
    app: recommendations
    version: {{ .Chart.Version }}
spec:
  type: ClusterIP
  selector:
    app: recommendations
    version: {{ .Chart.Version }}
  ports:
  - port: 80
    name: http
