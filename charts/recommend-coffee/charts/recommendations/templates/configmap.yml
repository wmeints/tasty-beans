apiVersion: v1
kind: ConfigMap
metadata:
  name: recommendations-config
data:
    telemetry.endpoint: "http://jaeger-collector.istio-system:14268/api/traces"
    telemetry.name: recommendations.default
    telemetry.version: "{{ .Chart.Version }}"